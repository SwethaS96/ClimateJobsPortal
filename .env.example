# Copy to .env and fill in real values. .env is gitignored — never commit credentials.

SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=

# Safety cap on how many notifications one digest email includes.
EMAIL_MAX_NOTIFICATIONS=200
